/**
 * Staff auth layout — clean, no carousel background.
 * Applies to /auth/staff/* routes only.
 */
export default function StaffAuthLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
